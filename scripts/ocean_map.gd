extends Node2D
#
#@onready var water_tile_map = $WaterTileMap
#
#var scroll_speed: Vector2 = Vector2(50, 50) # Diagonal speed (pixels per second)
#
#func _process(delta):
	## Access and adjust the offset of layer 0
	#var current_offset = water_tile_map.get_layer_property(0, "tile_origin_offset")
	#water_tile_map.set_layer_property(0, "tile_origin_offset", current_offset + scroll_speed * delta)
